# Railway 대시보드 > Variables 탭에 아래 값들을 입력하세요

# SQLite 파일 경로 (Volume 마운트 경로에 저장)
DB_PATH=/data/billiards.db

# 포트 (Railway가 자동 설정하므로 건드리지 않아도 됨)
# PORT=8787

# CORS 허용 도메인 (your-domain.com 을 실제 도메인으로 교체)
CORS_ORIGIN=https://billiards.your-domain.com
